type WeatherDifference @goModel(model: "github.com/ArtemGretsov/golang-exchanges-rates/graph/model.WeatherDifference") {
    id: ID!
    temperature: String!
    pressure: String!
}

type Weather @goModel(model: "github.com/ArtemGretsov/golang-exchanges-rates/graph/model.Weather") {
    id: ID!
    temperature: Int!
    pressure: Int!
    difference: WeatherDifference!
}